export enum ContactType {
  Phone,
  Email,
}

export enum MaterialTransactionType {
  Add,
  Substruct,
}

export enum Role {
  Admin,
  Owner,
  Accountant,
  Employee,
  Guest,
}

export enum Sex {
  Male,
  Female,
}
